- Entscheidung: `generate-recipe` und `preferences-page` bleiben getrennt — der Datenfluss läuft über die Root-Services, nicht über eine zusammengelegte Component.
- Kompletter Flow festgelegt: `/generate-recipe` → `/preferences` → Submit → `/recipe-results` (Animation, dann 3 Karten) → „View" → `/recipe/:id` aus Supabase.
- n8n speichert die 3 generierten Rezepte selbst in Supabase; die UUID kommt von `gen_random_uuid()` und wird über die Response ans Frontend zurückgegeben.
- 14 Step-Issues #28–#41 angelegt (Label `enhancement`, mit Abhängigkeiten verlinkt); #27 geschlossen und auf #34, #35, #37, #38 verwiesen.
- Mapping `snake_case` → `camelCase` verworfen: Interfaces spiegeln die DB-Form direkt, damit entfällt eine Fehlerquelle. #31 geschlossen.
- Tabellen bleiben `snake_case` — camelCase-Spalten müssten in Postgres überall gequotet werden.
- Interfaces umbenannt und `-interface`-Suffix entfernt; `recipe-response.ts` nach `models/` verschoben (Unions und Response-Wrapper dort, DB-/Payload-Formen in `interfaces/`).
- Offen in `interfaces/`: `dish-preferences.ts` wird durch `payload.ts` ersetzt und danach gelöscht.
- Step 05 (#32) begonnen: Webhook-Node muss auf „Respond to Webhook node" stehen, Structured-Output-Schema in snake_case ohne `id`, jsonb-Felder am Stück als JSON an den Supabase-Node.
- n8n-eigener Timeout kann kein `{ status: 'error' }` mehr senden — dafür braucht das Frontend einen eigenen `AbortSignal.timeout()` (#34).
- Noch zu klären: „zu wenig Zutaten" per fester Untergrenze in n8n, per LLM-Urteil oder als Kombination aus beidem.
