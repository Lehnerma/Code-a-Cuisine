- CI-Workflows in den Repository-Stamm verschoben, damit GitHub Actions sie erkennt.
- Root-Skripte und ESLint-Ignorierung von `poll-app` auf `code-a-cuisine` angepasst.
- Die maximale Funktionslänge gilt nicht für Vitest-Spezifikationen, damit Test-Setup-Code die CI nicht blockiert.
