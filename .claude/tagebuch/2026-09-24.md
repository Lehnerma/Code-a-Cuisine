- Dialog öffnet sich automatisch (`afterNextRender` + `showModal()`), × und Zurück-Button führen über `actionRoute` nach `/generate-recipe/preferences`
- `recipe-results`: ein `dialogContent`-Computed (insufficient = Mockup-Text, Ladefehler/`status: error` = "Service currently unavailable"), Test-Dialog + Open-Button entfernt
- Worktree-Guard per `bgIsolation: none` in `settings.local.json` abgeschaltet (Max arbeitet direkt im Projekt)
- `recipe-results`: `viewState`-Computed (loading/timeout/loadError/insufficient/error/success/idle) + ein `@switch` mit eigenem Dialog pro Fall; Timeout wird über `TimeoutError` erkannt, `@default` zeigt Fehler-Dialog
